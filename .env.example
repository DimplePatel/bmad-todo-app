# Copy to .env for `docker compose up` to pick these up automatically.

# --- Backend runtime ---
NODE_ENV=development
PORT=3001
DATABASE_PATH=./data/todos.db
CORS_ORIGIN=http://localhost:5173

# --- Compose host ports ---
BACKEND_PORT=3001
FRONTEND_PORT=5173
